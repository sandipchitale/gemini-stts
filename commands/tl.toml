prompt = """
## Step 1: Capture spoken prompt

First iteration only — read the transcription from this pre-exec:

!{EXT=$(node -e "const os=require('os'),fs=require('fs'),p=require('path');const d=p.join(os.homedir(),'.gemini/extensions/gemini-stts');const m=JSON.parse(fs.readFileSync(p.join(d,'.gemini-extension-install.json'),'utf8'));console.log(m.type==='link'?m.source:d)") && node "$EXT/dist/stt.mjs" --start-recording {{args}} }

On subsequent iterations (when looping back from Step 5), DO NOT rely on the above pre-exec — it only runs once when the slash command is first invoked. Instead, capture the next prompt yourself by calling the Bash tool with:

```
EXT=$(node -e "const os=require('os'),fs=require('fs'),p=require('path');const d=p.join(os.homedir(),'.gemini/extensions/gemini-stts');const m=JSON.parse(fs.readFileSync(p.join(d,'.gemini-extension-install.json'),'utf8'));console.log(m.type==='link'?m.source:d)") && node "$EXT/dist/stt.mjs" --start-recording
```

## Step 2: If the captured prompt is an empty string or the command exited with non-zero status, exit the loop. Do not output anything at all.

## Step 3: You must output only the prompt returned by the above command to the output first.

## Step 4: Then submit it to the model as the user-entered prompt.

## Step 5: **MANDATORY** Go back to Step 1 above and re-capture via the Bash tool invocation shown there. Keep looping until Step 2 exits.
"""
